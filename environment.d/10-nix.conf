# Nix profile PATH and XDG environment for systemd & GUI session
PATH=${HOME}/.nix-profile/bin:${HOME}/.local/bin:${PATH}
XDG_DATA_DIRS=${HOME}/.nix-profile/share:${XDG_DATA_DIRS:-/usr/local/share/:/usr/share/}
